#remove the .tml and save it as ".env" file


AI_PROVIDER=openai
AI_MODEL=gpt-4o
OPENAI_API_KEY= 
ERP_URL=http://127.0.0.1:8000
ERP_API_KEY=
ERP_API_SECRET=
ENVIRONMENT=development #use ENVIRONMENT=development to see the generated SQL section and use production to hide it in prod server.
#DATABASE_URL=postgresql://user:password@localhost/dbname #in case we need to connect to postgres database.